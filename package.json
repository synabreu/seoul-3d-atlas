{
  "name": "seoul-3d-atlas-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node --check dist/app.js && node --check dist/hiking.js && node --check dist/apt-model.js && node --check dist/apt-prices.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
